# ============================================
# 前端环境变量（Vite 规则：只有 VITE_ 前缀的会暴露给客户端代码）
# ============================================
# 使用方式：
#   1. 复制本文件为 frontend/.env 或 frontend/.env.local，按需修改
#   2. dev / build 重启后生效（vite 在启动时一次性读取）
#   3. 在 .vue / .js 中通过 import.meta.env.VITE_APP_NAME 读取
#   4. HTML <title> 由 vite.config.js 的 MpaPlugin.inject 注入

# 平台展示名——用于：
#   - 浏览器 tab 标题（admin / login 两个入口）
#   - 登录页标题与品牌名
#   - 后台侧栏品牌名
#   - dashboard 欢迎横幅
# 建议与后端 APP_NAME 保持一致
VITE_APP_NAME=智慧AI 探索平台
